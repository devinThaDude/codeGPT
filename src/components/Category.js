import React from 'react';
import './Category.css';
import Topic from '../Topic/Topic';

function Category({ category }) {
  return (
    <div className="Category">
      {/* Render Topic components for each topic in the category */}
    </div>
  );
}

export default Category;
