import React from 'react';
import './Topic.css';
import Post from '../Post/Post';
import Reply from '../Reply/Reply';

function Topic({ topic }) {
  return (
    <div className="Topic">
      {/* Render Post component for the original post */}
      {/* Render Reply components for each reply */}
    </div>
  );
}

export default Topic;
